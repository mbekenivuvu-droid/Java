// Arrays: Store mutiple vaues//
let color = [];

// push method: adds items tto the end of the array//
color.push("Blue");
color.push("Green");
color.push("Purple");

let numbers = [9, 18, 27, 36, 45];

//pop method: removes the last item//
color.pop();

//unsift method: adds on items to the begining//
color.unshift("Black");

//console.log: prints vaues to the console for debugging, helps see what is happening in your code//
// only property arrays have is length//
console.log("Length of colors array", color.length);
console.log("Length of numbers array", numbers.length);